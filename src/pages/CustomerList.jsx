import { useEffect } from "react"

export const CustomerList = ({ }) => {
   

    return (
        <section className="chart-list flex column ">
            <h1>Customer List</h1>
        </section>
    )
}